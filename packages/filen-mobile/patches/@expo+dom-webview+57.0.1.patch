diff --git a/node_modules/@expo/dom-webview/android/src/main/java/expo/modules/webview/DomWebView.kt b/node_modules/@expo/dom-webview/android/src/main/java/expo/modules/webview/DomWebView.kt
index 125ad1d..0f32890 100644
--- a/node_modules/@expo/dom-webview/android/src/main/java/expo/modules/webview/DomWebView.kt
+++ b/node_modules/@expo/dom-webview/android/src/main/java/expo/modules/webview/DomWebView.kt
@@ -198,6 +198,13 @@ internal class DomWebView(context: Context, appContext: AppContext) : ExpoView(c
       // API 30+ default disables file access; DOM bundles need it for sibling assets.
       settings.allowFileAccess = true
       settings.allowFileAccessFromFileURLs = true
+      // Patched (filen): builtInZoomControls defaults to false, which disables Chromium's
+      // multi-touch zoom processing entirely — no page can pinch-zoom regardless of its
+      // viewport. Enable it (with the on-screen +/- controls hidden); pages keep control
+      // via their viewport meta, and the DOM-component shell ships user-scalable=no, so
+      // only components that explicitly relax it (docxPreview) become zoomable.
+      settings.builtInZoomControls = true
+      settings.displayZoomControls = false
       webViewClient = createWebViewClient()
       addJavascriptInterface(rncWebViewBridge, "ReactNativeWebView")
       addJavascriptInterface(DomWebViewBridge(this@DomWebView), "ExpoDomWebViewBridge")
diff --git a/node_modules/@expo/dom-webview/src/DomWebView.tsx b/node_modules/@expo/dom-webview/src/DomWebView.tsx
index 3a07ac9..f1ecde1 100644
--- a/node_modules/@expo/dom-webview/src/DomWebView.tsx
+++ b/node_modules/@expo/dom-webview/src/DomWebView.tsx
@@ -23,7 +23,13 @@ const WebView = React.forwardRef<DomWebViewRef, DomWebViewProps>(
       ref,
       () => ({
         scrollTo: (params) => viewRef.current?.scrollTo(params),
-        injectJavaScript: (script: string) => viewRef.current?.injectJavaScript(script),
+        // Patched (filen): the native injectJavaScript is an expo-modules AsyncFunction whose Promise
+        // rejects when the DomWebView is mid-mount/destroyed (Android view-tag race on cold note-open).
+        // The call is fire-and-forget; an uncaught rejection otherwise hits our prod diagnostic logger
+        // on every racey open. Swallow it — nothing to inject into a gone view, and in-page JS errors
+        // are not reported through this Promise (evaluateJavascript discards results).
+        injectJavaScript: (script: string) =>
+          void (viewRef.current?.injectJavaScript(script) as unknown as Promise<void> | undefined)?.catch(() => {}),
         reload: () => viewRef.current?.reload(),
       }),
       []
