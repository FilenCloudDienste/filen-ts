diff --git a/android/src/main/java/expo/modules/sharing/SharingModule.kt b/android/src/main/java/expo/modules/sharing/SharingModule.kt
index 69ccba68e28af71a262c01bd54a202a7bd985909..3fbce27789789d3d146d73699fd19527b5d9ba85 100644
--- a/android/src/main/java/expo/modules/sharing/SharingModule.kt
+++ b/android/src/main/java/expo/modules/sharing/SharingModule.kt
@@ -23,9 +23,11 @@ class SharingModule : Module() {
     Name("ExpoSharing")
 
     AsyncFunction("shareAsync") { url: String?, params: SharingOptions, promise: Promise ->
-      if (pendingPromise != null) {
-        throw SharingInProgressException()
-      }
+      // A stale pending promise means the previous chooser's activity result was never
+      // delivered (seen on newer Android sharesheets) — rejecting here would wedge every
+      // further share for the whole process. Settle it and let the new share proceed.
+      pendingPromise?.resolve(null)
+      pendingPromise = null
       try {
         val fileToShare = getLocalFileFoUrl(url)
         val contentUri = FileProvider.getUriForFile(
diff --git a/android/src/main/java/expo/modules/sharing/dataParsers/ResolvingShareIntentDataParser.kt b/android/src/main/java/expo/modules/sharing/dataParsers/ResolvingShareIntentDataParser.kt
index 5f4d9423acc2999c1f1209181c95c37138155b5d..93d30b33c93a8a100191bb6c5fadba1e28469898 100644
--- a/android/src/main/java/expo/modules/sharing/dataParsers/ResolvingShareIntentDataParser.kt
+++ b/android/src/main/java/expo/modules/sharing/dataParsers/ResolvingShareIntentDataParser.kt
@@ -25,11 +25,20 @@ class ResolvingShareIntentDataParser {
     }
 
     private fun handleSendAction(context: Context, intent: Intent, type: String): List<ResolvedSharePayload> {
+      // Patched (filen): mirror of the SimpleShareIntentDataParser fix - a text/plain share that
+      // carries a file in EXTRA_STREAM (no EXTRA_TEXT) was dropped to an empty list, wedging
+      // receivers waiting on resolution. Stream first, text handling only without one.
+      // Upstreamable.
+      val streamUri = intent.getParcelableExtraCompat<Uri>(Intent.EXTRA_STREAM)
+
+      if (streamUri != null) {
+        return listOf(resolveUri(context, streamUri, type))
+      }
+
       return if (type == "text/plain") {
         extractTextPayload(intent)
       } else {
-        val uri = intent.getParcelableExtraCompat<Uri>(Intent.EXTRA_STREAM)
-        listOfNotNull(uri?.let { resolveUri(context, it, type) })
+        emptyList()
       }
     }
 
diff --git a/android/src/main/java/expo/modules/sharing/dataParsers/SimpleShareIntentDataParser.kt b/android/src/main/java/expo/modules/sharing/dataParsers/SimpleShareIntentDataParser.kt
index 8dd50e8f04022d183884a57e4d61a7e1711b9e49..0f0e4db81f0280b4a8a5148edb74c1ed21a602e4 100644
--- a/android/src/main/java/expo/modules/sharing/dataParsers/SimpleShareIntentDataParser.kt
+++ b/android/src/main/java/expo/modules/sharing/dataParsers/SimpleShareIntentDataParser.kt
@@ -17,6 +17,17 @@ internal class SimpleShareIntentDataParser {
     }
 
     private fun handleSendAction(context: Context, intent: Intent, type: String): List<SharePayload> {
+      // Patched (filen): file shares can arrive with ANY mime type - senders like Files by Google
+      // stamp text-like files (.log/.json/.py/...) as text/plain - carrying the file in
+      // EXTRA_STREAM with no EXTRA_TEXT. The text/plain branch only read EXTRA_TEXT and returned
+      // an empty list for those, silently dropping the share. Prefer the stream whenever present;
+      // the text branch only applies to genuine text-only shares. Upstreamable.
+      val streamUri = intent.getParcelableExtraCompat<Uri>(Intent.EXTRA_STREAM)
+
+      if (streamUri != null) {
+        return listOf(createUriPayload(context, streamUri, type))
+      }
+
       return if (type == "text/plain") {
         val text = intent.getStringExtra(Intent.EXTRA_TEXT) ?: return emptyList()
         val isUrl = android.util.Patterns.WEB_URL.matcher(text).matches()
@@ -29,8 +40,7 @@ internal class SimpleShareIntentDataParser {
           }
         )
       } else {
-        val uri = intent.getParcelableExtraCompat<Uri>(Intent.EXTRA_STREAM)
-        listOfNotNull(uri?.let { createUriPayload(context, it, type) })
+        emptyList()
       }
     }
 
