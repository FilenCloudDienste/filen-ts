{
	"_meta": {
		"created": "2026-03-02",
		"updated": "2026-09-24",
		"note": "Covers all packages: filen-mobile, filen-web, filen-desktop, filen-shared. Edit only to fix a rule the code contradicts.",
		"configSources": [".prettierrc", ".editorconfig", "packages/*/eslint.config.mjs", "packages/filen-web/eslint.config.js"]
	},
	"indentation": {
		"style": "tabs",
		"size": 4,
		"overrides": {
			"*.yaml": {
				"style": "spaces",
				"size": 2
			},
			"*.yml": {
				"style": "spaces",
				"size": 2
			}
		}
	},
	"quotes": {
		"ts": "double",
		"tsx": "double",
		"js": "double",
		"jsx": "double",
		"jsxAttributes": "double",
		"yaml": "single"
	},
	"semicolons": false,
	"trailingCommas": "none",
	"bracketSpacing": true,
	"bracketSameLine": false,
	"arrowParens": "avoid",
	"singleAttributePerLine": true,
	"printWidth": 140,
	"endOfLine": "lf",
	"packageManager": "pnpm",
	"typescript": {
		"typeImports": "type-keyword-inline",
		"notes": "Mobile/shared: 'import { type Foo }' is the documented form (whole-statement 'import type' also appears). filen-web: whole-statement 'import type { Foo }' when a module is imported for types only (verbatimModuleSyntax keeps an all-inline-type import as a side-effect import); inline 'import { x, type T }' when mixed"
	},
	"imports": {
		"style": "named-preferred",
		"multiLineEachOnOwnLine": true,
		"noTrailingComma": true,
		"groupOrder": "none enforced",
		"groupSeparator": "none",
		"notes": [
			"filen-mobile and filen-web: relative imports (./foo) are FORBIDDEN by ESLint — always use @/ aliases",
			"filen-desktop and filen-shared: relative imports are allowed",
			"No blank line between external and @/ imports (web lists externals first; mobile interleaves). Match the file being edited",
			"Type-only import form: see typescript.notes"
		]
	},
	"exports": {
		"style": "named-and-default",
		"notes": "Most files export named items + a default export at the bottom. Store files export both named store and default."
	},
	"emptyLines": {
		"betweenFunctions": 1,
		"betweenImportGroups": 0,
		"atTopOfBlock": 0,
		"beforeReturn": 1,
		"afterMultiLineDeclaration": 1
	},
	"jsx": {
		"closingBracketOnNewLine": true,
		"eachPropOnOwnLine": true,
		"notes": "When a JSX element has multiple props, each prop goes on its own indented line; closing > is on its own line aligned with the opening tag"
	},
	"ternaries": {
		"style": "multiline-when-complex",
		"notes": "Simple one-liners stay inline; complex/nested ternaries break to multiple lines with ? and : at start of new lines"
	},
	"comments": {
		"inline": "// ",
		"todo": "// TODO: ",
		"block": "/* */"
	},
	"eslint": {
		"eqeqeq": true,
		"noDuplicateImports": true,
		"unusedVarsIgnorePrefix": "_",
		"reactCompiler": "enabled in filen-mobile and filen-web"
	},
	"perPackage": {
		"filen-mobile": {
			"pathAliases": "@/ → src/",
			"relativeImports": "forbidden",
			"framework": "React Native + Expo",
			"storePattern": "useCameraUpload.store.ts — named export + default export, Zustand create()",
			"queryPattern": "useX.query.ts — named fetchData + named useXQuery + named xQueryUpdate + default export"
		},
		"filen-web": {
			"pathAliases": "@/ → src/",
			"relativeImports": "forbidden",
			"framework": "React + Vite",
			"nonNull": "no-non-null-assertion AND non-nullable-type-assertion-style are both on, so neither '!' nor a bare null-stripping 'x as T' passes. Narrow with a guard (if (x === undefined) throw/return/continue); for hot bounds-checked indexing use a local throwing at<T>(arr, i) helper, as in packages/filen-shared/src/driveSortEngine.ts",
			"tsconfig": "verbatimModuleSyntax, exactOptionalPropertyTypes, noUncheckedIndexedAccess on",
			"lint": "typescript-eslint strictTypeChecked + stylisticTypeChecked; prettier --check is part of pnpm run lint"
		},
		"filen-desktop": {
			"relativeImports": "allowed",
			"framework": "Electron"
		},
		"filen-shared": {
			"relativeImports": "allowed",
			"testFramework": "vitest",
			"testFiles": "src/tests/*.test.ts"
		}
	},
	"callSiteStyle": {
		"objectArguments": "always-multiline",
		"notes": [
			"Single-property objects passed as arguments are still expanded to multiple lines: run(fn, {\n\tthrow: true\n})",
			"Inline defer callbacks are forbidden — always expand the body: defer(() => {\n\tthis.x.release()\n})"
		]
	},
	"notes": [
		"Tabs everywhere for TS/TSX/JS — not spaces",
		"No semicolons, no trailing commas — both confirmed across all packages",
		"Double quotes for all JS/TS string literals and JSX attribute values",
		"Arrow functions omit parens for single params: 'x => x', not '(x) => x'",
		"Classes are common for singletons (Auth, CameraUpload, Transfers) — exported as instance",
		"Zustand stores: functional setState pattern with updater functions (fn | ((prev) => fn(prev)))",
		"Never use non-null assertion '!'. Try to handle null/undefined cases explicitly instead of asserting non-null. If impossible, use explicit 'as Type' cast when the type is known to be non-null, rather than '!' (not in filen-web: see perPackage.filen-web.nonNull).",
		"Use fastLocaleCompare() from @filen/shared instead of String.prototype.localeCompare() for filen-mobile and filen-web — it has better performance and consistent behavior across platforms",
		"Use exposed run<X>() functions from @filen/shared for rust-style RAII wrappers",
		"When functions have params, always use objects for better readability and easier future extension, except for when the function only has a single parameter"
	]
}
